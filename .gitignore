# Four-player table replays (see simulate-table skill).
# Replay logs are scratch by default; commit a game deliberately with `git add -f`.
table-games/*.json
table-games/*.live.json
table-games/*.conduit.json
table-games/*.runner.json
table-games/*.runner.log
table-games/*.runner.pid
table-games/*.invite-p*.json

# GitHub Pages build output.
node_modules/
dist/
site/public/games.json
site/public/replays/
site/public/decks/
